{
  "name": "dim",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "devDependencies": {
    "@types/three": "^0.137.0",
    "@types/webxr": "^0.2.3",
    "esbuild": "^0.14.22",
    "typescript": "^4.5.5"
  },
  "dependencies": {
    "three": "^0.137.5"
  },
  "scripts": {
    "dev": "cp -r public/* dist && esbuild src/main.ts --bundle --sourcemap --servedir=dist --outfile=dist/dim.js",
    "build": "cp -r public/* dist && esbuild src/main.ts --bundle --minify --sourcemap --outfile=dist/dim.js",
    "deploy": "aws s3 sync dist s3://dimlit.xyz"
  },
  "prettier": {
    "singleQuote": true
  }
}
